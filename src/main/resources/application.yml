server:
  port: 8888
spring:
  application:
    name: personal-finance-app
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:mysql}  # Reads from .env file, defaults to mysql
  # Database configuration is now in profile-specific files:
  # - application-mysql.yml
  # - application-mysql.yml